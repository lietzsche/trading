spring:
  profiles:
    active:
    - prd
  application:
    name: trade-service
  jpa:
    show-sql: true
    generate-ddl: true
    database: postgresql
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${DB_USERNAME:bion_user}
    password: ${DB_PASSWORD}
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
  messages:
    encoding: UTF-8
server:
  port: 8080
  # Cloudflare Quick Tunnel terminates TLS and forwards the original scheme.
  # Honor that header so redirects and generated asset URLs remain HTTPS.
  forward-headers-strategy: framework

management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include:
        - health
        - info
        - metrics
        - httpexchanges
        - logfile
  endpoint:
    health:
      show-details: never
    env:
      show-values: never
    logfile:
      access: read-only

calculation-service:
  url: ${CALCULATION_SERVICE_URL:http://localhost:8000}

logging:
  file:
    name: ../logfile.log  
  logback:
    rollingpolicy:
      max-history: 1
      max-file-size: 100MB
      file-name-pattern: ${LOG_FILE}.%d{yyyy-MM-dd}-%i.log
  charset:
    file: UTF-8
  pattern:
    dateformat: yyyy-MM-dd HH:mm:ss.SSS,Asia/Seoul

info:
  contact:
    email: stxtory@gmail.com
    phone: 010-2397-7156
